# 실습용 웹 애플리케이션 보안 리뷰

2026. 9. 21. · PHP / MySQL · 동아리 활동

동아리에서 만든 웹 애플리케이션의 코드를 살펴봤다. 로그인과 검색에서는 사용자 입력을 SQL에 직접 넣고 있었고, 댓글 삭제에서는 작성자 확인이 빠져 있었다. 계정을 삭제한 뒤에도 기존 세션을 그대로 믿는 부분도 있었다.

이 글은 소스 검토 기록이다. 아래 수정 코드는 제안이며 실제 서버에 적용하거나 전체 기능을 재시험한 것은 아니다. 사이트 주소와 계정 정보, 공격 입력은 제외했다. 코드 예시는 원본에서 필요한 부분만 발췌했다.

## 로그인: 아이디가 SQL 문장 안에 들어간다

로그인 처리의 취약 모드 분기는 다음과 같았다.

```php
$username = $_POST['username'] ?? '';
$password = $_POST['password'] ?? '';

$sql = "SELECT * FROM users WHERE username = '$username' LIMIT 1";
$user = $pdo->query($sql)->fetch();
```

문제가 되는 부분은 `username = '$username'`이다. PHP가 문자열을 만들 때 입력값을 먼저 끼워 넣고, 완성된 SQL을 DB에 전달한다. DB 입장에서는 어떤 부분이 개발자가 쓴 SQL이고 어떤 부분이 사용자가 입력한 값인지 구별할 수 없다.

예를 들어 아이디에 작은따옴표가 들어가면 SQL에서 문자열을 닫는 따옴표와 충돌할 수 있다. 여기에 SQL 문법이 이어지면 단순한 아이디 조회가 아니라 다른 조건의 질의가 된다. 입력값이 쿼리의 구조를 바꿀 수 있다는 점이 SQL 인젝션의 원인이다.

원본은 쿼리에서 예외가 나면 일반적인 로그인 실패로 처리했다. 하지만 오류 문구를 숨기는 것과 SQL이 변조되지 않게 만드는 것은 별개다.

### 그런데 왜 로그인 우회 시험은 실패했을까?

조회 다음에 비밀번호 검사가 한 번 더 있었다.

```php
if ($user && password_verify($password, $user['password'])) {
    session_regenerate_id(true);
    $_SESSION['user_id'] = $user['id'];
    $_SESSION['is_admin'] = $user['is_admin'];
}
```

조회 조건만 바꿔서 기존 계정이 선택되더라도 그 계정의 비밀번호 검증을 통과해야 한다. 앞선 제한적인 웹 시험에서 로그인되지 않은 결과는 이 코드와 맞아떨어진다.

그렇다고 SQL 인젝션이 없는 것은 아니다. 인증에 사용할 사용자 행을 가져오는 과정부터 조작될 수 있다. 특히 결과 행의 비밀번호 해시와 권한 값까지 바뀔 수 있는 상황이라면 후속 검증도 믿기 어렵다. 이번 소스 검토에서 실제 관리자 로그인 성공까지 재현한 것은 아니다.

### 수정 예시

```php
$stmt = $pdo->prepare(
    'SELECT * FROM users WHERE username = ? LIMIT 1'
);
$stmt->execute([$username]);
$user = $stmt->fetch();
```

SQL에는 값 대신 `?`를 두고, 아이디는 `execute()`에 따로 전달한다. 입력이 SQL 문법에 섞이지 않도록 PDO의 매개변수 바인딩을 사용하는 방식이다. 조회 뒤의 비밀번호 검증과 세션 ID 재생성은 그대로 유지한다.

원본에도 안전 모드 분기에 이 방식이 구현돼 있었다. 취약한 분기를 없애고 항상 바인딩하는 쪽으로 정리하면 된다.

## 게시판 검색: `trim()`으로는 막을 수 없다

검색에서도 같은 문제가 반복됐다.

```php
$q = trim($_GET['q'] ?? '');

$sql = "SELECT * FROM posts
        WHERE title LIKE '%$q%' OR content LIKE '%$q%'
        ORDER BY id DESC";
$posts = $pdo->query($sql)->fetchAll();
```

검색어 `$q`가 제목과 본문 조건에 직접 들어간다. `trim()`은 입력 양끝의 공백 등을 제거할 뿐, 입력을 SQL 데이터로 분리해 주지 않는다. `%`로 검색어를 감싸도 마찬가지다. `%`는 `LIKE` 검색에서 사용하는 와일드카드일 뿐 보안 처리가 아니다.

이 검색 기능은 로그인을 요구하지 않는다. 따라서 코드를 기준으로 보면 비로그인 사용자도 문제의 쿼리에 입력을 전달할 수 있다. 검색 결과가 화면에 출력되므로, 쿼리를 조작할 수 있다면 DB 권한이 허용하는 다른 데이터가 노출되는 통로가 될 수 있다. 실제 유출은 이번 검토에서 실행하지 않았다.

### 수정 예시

```php
$stmt = $pdo->prepare(
    'SELECT * FROM posts
     WHERE title LIKE ? OR content LIKE ?
     ORDER BY id DESC'
);
$pattern = '%' . $q . '%';
$stmt->execute([$pattern, $pattern]);
$posts = $stmt->fetchAll();
```

와일드카드를 포함한 검색 문자열 전체를 값으로 전달한다. 이렇게 하면 검색어 안의 따옴표가 SQL 구조를 바꾸지 않는다. 검색어에 포함된 `%`와 `_`를 와일드카드로 허용할지는 별도의 검색 정책이다.

출력할 때 쓰는 `htmlspecialchars()`는 HTML 처리용이다. 검색 결과를 안전하게 출력하는 데 필요하지만, 이미 실행된 SQL의 문제를 해결하지는 않는다.

## 댓글 삭제: 로그인했다고 남의 댓글을 지울 수는 없다

댓글 삭제 쿼리는 바인딩을 사용하고 있었다. 여기서는 SQL 인젝션보다 권한 검사가 문제였다.

```php
$pdo->prepare('DELETE FROM comments WHERE id=?')->execute([$cid]);
```

앞에서 로그인 여부만 확인하고, 그 댓글을 작성한 사람인지 확인하지 않았다. 쿼리를 안전하게 실행해도 대상에 대한 권한 검사가 없으면 다른 사람의 댓글을 삭제할 수 있다.

수정할 때는 댓글 ID뿐 아니라 로그인한 사용자 ID와 게시글 ID도 조건에 넣어야 한다. 삭제 요청은 POST로 받고 CSRF 검증도 해야 한다. 화면에서 삭제 버튼을 감추는 것만으로는 부족하다.

## 프로필 조회는 공개 범위를 먼저 정해야 한다

취약 모드에서는 로그인한 사용자가 다른 회원을 지정해 프로필을 조회할 수 있었다. 자기소개가 본인만 보는 정보라면 문제가 된다. 반대로 공개 프로필로 설계한 기능이라면 다른 회원이 조회한다는 사실만으로 취약점이라고 할 수는 없다.

프로필 수정에는 본인 확인 코드가 있었다. 따라서 조회 문제를 다른 회원의 프로필 수정까지 가능한 문제로 확대해서 적지는 않았다.

## 삭제 요청과 CSRF

게시물과 댓글, 관리자 기능 일부가 GET 요청으로 삭제를 처리했다. POST를 사용하는 상태 변경 기능에도 CSRF 토큰이나 요청 출처를 검증하는 코드는 없었다.

피해자 브라우저가 세션 쿠키를 함께 보내면 그 사용자의 권한으로 처리될 수 있다. 실제 교차 사이트 요청의 성공 여부는 쿠키 정책 등에도 영향을 받지만, 애플리케이션에 방어가 빠져 있는 것은 확인할 수 있었다.

삭제는 POST로 제한하고 서버에서 CSRF 토큰을 검증해야 한다. 삭제 버튼의 확인창은 해당 버튼을 누를 때만 동작하므로 서버의 검증을 대신하지 못한다.

## 계정을 삭제해도 세션은 남을 수 있다

로그인 여부는 세션에 사용자 ID가 있는지만 확인했다. 관리자 여부도 로그인할 때 저장한 값을 그대로 사용했다.

이 구조에서는 DB의 계정 행을 삭제해도 다른 브라우저에 남아 있는 세션은 계속 로그인 상태로 취급될 수 있다. 관리자 권한을 변경한 경우도 마찬가지다. 요청 시 현재 계정 상태를 확인하거나, 계정 삭제·권한 변경 시 모든 세션을 무효화하는 처리가 필요하다.

## 그 밖에 확인한 부분

- **비밀번호 정책:** 비밀번호를 해시로 저장하고 있었지만, 가입 단계에서는 빈 값만 검사했다. 안전한 저장과 약한 비밀번호 차단은 따로 처리해야 한다.
- **오류 정보:** DB 연결 실패 시 예외 메시지를 그대로 화면에 보여줬다. 상세 내용은 서버 로그에 남기고 방문자에게는 일반적인 오류만 보여주는 편이 낫다.
- **이미 적용된 방어:** 검토한 게시물·댓글 출력에는 HTML 이스케이프가 있었다. 로그인 성공 시 세션 ID도 재생성했다. 게시물 수정·삭제에는 작성자 또는 관리자 확인이 있어 댓글 삭제와 구분했다.

## 수정 후 다시 볼 것

SQL 인젝션은 로그인과 검색의 바인딩부터 확인해야 한다. 그다음 시험 계정 두 개로 서로의 댓글을 삭제할 수 없는지, 계정 삭제 후 기존 세션이 차단되는지 확인할 계획이다. GET 요청이나 잘못된 CSRF 토큰으로 데이터가 바뀌지 않는지도 확인해야 한다.

취약 모드를 끄면 SQL 인젝션 등 일부 분기는 바뀌지만 CSRF와 세션 문제는 남는다. 설정 하나를 바꿨다는 이유만으로 수정이 끝났다고 보기는 어렵다.

현재까지는 코드 검토와 수정 방향 정리 단계다. 실제 수정 및 회귀 시험 결과는 아직 포함하지 않았다.

## 참고

- [OWASP SQL Injection Prevention](https://cheatsheetseries.owasp.org/cheatsheets/SQL_Injection_Prevention_Cheat_Sheet.html)
- [OWASP CSRF Prevention](https://cheatsheetseries.owasp.org/cheatsheets/Cross-Site_Request_Forgery_Prevention_Cheat_Sheet.html)
