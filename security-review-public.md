# 동아리 활동

## 실습용 웹 애플리케이션 보안 코드 리뷰

2026년 9월 21일 · 공개용 활동 기록 · PHP / MySQL

동아리에서 직접 제작한 실습용 웹 애플리케이션을 대상으로, 사용자 입력이 데이터베이스와 세션에 전달되는 흐름을 검토했다. 이 글은 발견 개수보다 문제의 원인과 개선 방법을 공유하기 위한 기록이다.

대상 주소, 서비스명, 원본 파일명과 줄 번호, 계정 정보, 구체적인 공격 입력 및 재현 URL은 공개본에서 제외했다. 원본 애플리케이션과 설치 자료도 공개하지 않는다.

## 활동 범위와 판단 기준

PHP 16개와 SQL·CSS·안내 문서 각 1개, 총 19개 파일을 정적으로 검토했다. 입력 처리, 데이터베이스 질의, 객체별 권한 검사, 세션 관리와 출력 처리를 살펴봤다.

이 기록은 제공된 소스를 읽어 분석한 결과다. 운영 환경에서 모든 문제를 실행해 재현한 결과가 아니며, 수정 완료를 의미하지도 않는다. 서버 설정과 실제 배포 코드가 다르면 영향도 달라질 수 있다. 프로필 조회 문제는 해당 정보가 비공개라는 정책을 전제로 한다.

## 주요 발견 사항

| 항목 | 코드에서 확인한 문제 | 개선 방향 |
|---|---|---|
| 로그인 SQL 인젝션 | 아이디를 SQL 문자열에 직접 결합 | 매개변수 바인딩으로 쿼리와 데이터 분리 |
| 검색 SQL 인젝션 | 검색어를 SQL 조건에 직접 결합 | 바인딩된 검색 쿼리와 입력 길이 제한 |
| 댓글 삭제 권한 | 로그인만 확인하고 댓글 소유자 검사 생략 | 객체·소유자·상위 게시글 관계 검증 |
| 프로필 조회 권한 | 다른 회원의 정보를 지정해 조회 가능 | 공개 정책 확정 후 필드별 접근 제한 |
| CSRF 방어 | 상태 변경 요청의 출처·토큰 검증 부재 | POST와 서버 측 CSRF 검증 |
| 세션 무효화 | 삭제·권한 변경 전 세션 값을 계속 신뢰 | 최신 계정 상태 확인 및 전체 세션 폐기 |
| 비밀번호 정책 | 빈 값 외의 강도 검사 부재 | 서버 측 길이·흔한 비밀번호 검증 |
| 오류 정보 노출 | DB 연결 예외를 화면에 출력 | 일반 오류 응답과 보호된 상세 로그 분리 |

## 1. SQL 인젝션: 비밀번호 검증만으로 충분하지 않다

로그인과 검색 기능에서 입력값이 SQL 문자열에 직접 들어가는 흐름을 확인했다. 입력은 데이터로만 처리되어야 하는데, 쿼리의 문법에 영향을 줄 수 있는 구조였다.

로그인은 조회 후 비밀번호 해시를 검증하고 있었다. 따라서 단순히 조회 조건을 참으로 만드는 것만으로 로그인에 성공한다고 단정할 수는 없다. 그러나 인증에 사용하는 조회 결과 자체를 신뢰할 수 없다면, 후속 비밀번호 검증도 전체 인증 흐름의 안전성을 보장하지 못한다.

개선의 핵심은 모든 관련 질의에 매개변수 바인딩을 사용하는 것이다. 오류를 숨기거나 HTML을 이스케이프하는 것으로 SQL 인젝션이 해결되지는 않는다.

## 2. 접근 제어: 로그인과 소유권은 다르다

댓글 삭제는 로그인 상태만 확인하고 댓글 작성자 여부를 검사하지 않았다. SQL 인젝션을 막는 바인딩이 있어도 객체 권한 검사가 없으면 다른 사람의 데이터에 영향을 줄 수 있다.

서버는 요청마다 수행할 동작, 사용자 권한, 대상 객체의 소유 관계를 확인해야 한다. 화면에서 버튼을 숨기는 것은 서버 측 검사를 대신할 수 없다.

프로필은 공개 정책이 중요하다. 공개 프로필 조회 자체는 취약점이 아니다. 본인에게만 보여야 하는 필드가 다른 회원에게도 보일 때 접근 제어 문제로 평가해야 한다.

## 3. CSRF: 상태 변경에는 요청 검증이 필요하다

삭제를 일반 링크의 GET 요청으로 처리하는 흐름과, POST 요청에서 CSRF 토큰이나 요청 출처를 검사하지 않는 흐름을 확인했다.

피해자의 세션이 함께 전달되는 요청에서는 정상 사용자 권한 검사만으로 사용자의 의도까지 확인할 수 없다. 브라우저 쿠키 정책은 영향을 주지만, 특히 GET으로 데이터를 삭제하는 설계의 충분한 방어로 볼 수 없다.

삭제는 POST로 제한하고, 세션에 연결된 예측 불가능한 토큰을 서버에서 검증해야 한다. 회원탈퇴처럼 중요한 작업에는 최근 재인증도 고려한다. 화면의 확인 대화상자는 서버 측 검증을 대체하지 않는다.

## 4. 세션: 계정 삭제와 로그인 종료는 별개다

인증 함수가 로그인 당시의 세션 값만 확인했다. 계정이 삭제되거나 관리자 권한이 바뀌어도 기존 세션에는 이전 정보가 남을 수 있었다.

데이터베이스에서 계정을 삭제하는 것과 모든 브라우저의 세션을 무효화하는 것은 별도의 작업이다. 계정 상태와 현재 권한을 확인하거나, 중앙 세션 관리·계정별 세션 버전으로 변경을 반영해야 한다.

## 5. 비밀번호와 오류 처리

비밀번호 해시 저장은 구현돼 있었지만 가입 시 강도 검사는 부족했다. 안전한 저장 방식과 추측하기 어려운 비밀번호 정책은 서로 다른 방어다. 서버에서 비밀번호 정책을 검증하고 로그인 시도 제한도 함께 검토해야 한다.

DB 연결 실패 시 상세 예외를 화면에 보여주는 처리도 있었다. 외부에는 일반적인 오류 메시지를 반환하고, 운영자가 필요한 상세 정보는 접근이 제한된 로그에 남기는 편이 적절하다.

## 수정 후 확인할 기준

- 특수문자가 포함된 입력이 쿼리의 구조를 바꾸지 않는다.
- 사용자 A는 사용자 B의 댓글을 삭제할 수 없다.
- 비공개 프로필 필드는 허용된 사용자에게만 보인다.
- GET이나 잘못된 CSRF 토큰으로는 데이터가 변경되지 않는다.
- 계정 삭제·권한 변경 직후 기존 세션에도 제한이 반영된다.
- 약한 비밀번호는 서버에서 거부되고 정상 비밀번호는 사용할 수 있다.
- 오류 응답에 내부 연결 정보가 나타나지 않는다.
- 정상 사용자의 작성·조회·수정 기능은 계속 동작한다.

검증에는 격리된 복제본, 가상 데이터, 서로 다른 시험 계정을 사용한다. 이 목록은 검증 계획이며 실행 완료 기록은 아니다.

## 이번 활동에서 배운 점

실습용 취약 모드를 끄면 일부 분기는 개선되지만, 공통 인증·세션·요청 처리의 문제가 자동으로 해결되지는 않는다. 코드의 안전 여부는 기능마다 입력부터 상태 변경까지 따라가며 확인해야 한다.

또한 발견하지 못한 문제를 있다고 단정하지 않는 것이 중요하다. 검토한 출력 경로에는 HTML 이스케이프가 있었고, 비밀번호 해시 검증과 로그인 성공 시 세션 ID 재생성도 구현돼 있었다. 취약한 부분과 이미 적용된 방어를 함께 기록해야 개선 방향이 명확해진다.

## 참고 자료

- [OWASP SQL 인젝션 예방 지침](https://cheatsheetseries.owasp.org/cheatsheets/SQL_Injection_Prevention_Cheat_Sheet.html)
- [OWASP CSRF 예방 지침](https://cheatsheetseries.owasp.org/cheatsheets/Cross-Site_Request_Forgery_Prevention_Cheat_Sheet.html)

이 글은 허가된 동아리 실습의 학습 기록이다. 대상 식별 정보와 구체적인 공격 방법을 제외한 공개용 편집본이다.
